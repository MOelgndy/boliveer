/**
 * Boliveer Design System — token + motion entry.
 * Primitives live in components/primitives.
 */
export {
  mount,
  fadeRise,
  cinematicReveal,
  staggerContainer,
  staggerItem,
  easeOut,
  easeCinematic,
  springSoft,
  springSnappy,
} from "./motion";
